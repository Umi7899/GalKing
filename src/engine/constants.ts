// src/engine/constants.ts
// Shared tuning constants for progression system

/** Grammar mastery threshold for advancement (0-100). Grammar point is considered "mastered" when mastery >= this value. */
export const MASTERY_THRESHOLD = 50;
